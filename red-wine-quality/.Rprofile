# Set path to required Python
Sys.setenv(RENV_PATHS_CACHE = paste0(getwd(), 'renv/conda-env/python'))

# Set download method to libcurl, which is native to R
Sys.setenv(RENV_DOWNLOAD_METHOD = "libcurl")

# Activate the project library
source("renv/activate.R")
